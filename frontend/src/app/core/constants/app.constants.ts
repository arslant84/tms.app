/**
 * Application Constants
 *
 * Centralized constants to avoid hardcoded values across components.
 * Keep in sync with backend/utils/constants.py where applicable.
 */

// =============================================================================
// Password Configuration
// =============================================================================
export const PASSWORD_MIN_LENGTH = 15;
